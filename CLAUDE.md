See [AGENTS.md](AGENTS.md).

That file is the source of truth for project conventions and is read by
Claude Code, Codex, Cursor, Copilot, Gemini CLI and others. This file exists
only so tools that look for `CLAUDE.md` first are pointed at it rather than
working without context.
