## What changed, and why it was wrong before

<!-- One or two sentences. The subject is the defect or the finding, not the
     work you did. -->

## How it was verified

<!-- Not "tests pass". Which check, on what input, with what result. If the
     change adds a check, say how you confirmed it fails on the defect it
     names. -->

## Checklist

- [ ] Six suites pass from the repository root
- [ ] Verified by content, not by the absence of an error
- [ ] Any new check was confirmed to fail on a damaged input
- [ ] Numbers are generated by a script, with `n` attached, not transcribed
- [ ] Nothing importable moved from the root, or the reason is stated below
- [ ] No em dashes
- [ ] Labelled. Path-based ones are automatic; add `needs-rerun`,
      `spends-credit`, `provisional` or `blocked` if they apply
- [ ] The freeze holds: shipped examples still regenerate exactly, or the
      change is additive and says so

## Anything left undone

<!-- State it here rather than leaving it to be found. -->
